{
  "name": "vmess-serverless-final",
  "version": "1.0.0",
  "description": "Auto-download Xray",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "curl -L -o xray.zip https://github.com/XTLS/Xray-core/releases/download/v1.8.4/Xray-linux-64.zip && unzip -o xray.zip xray && rm xray.zip && chmod +x xray"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
